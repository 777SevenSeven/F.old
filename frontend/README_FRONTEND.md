# Frontend Guide

## Prerequisites

- Flutter SDK (stable)
- Chrome

## Install

```bash
flutter pub get
```

## Run

```bash
flutter run -d chrome
```

## Configuration

Edit `lib/config/api_config.dart` to point to your API base URL.
